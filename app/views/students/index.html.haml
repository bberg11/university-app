.row
  .col.s12
    %h1.center-align Students Directory

.row
  - @students.each do |student|
    .col.s6.m6
      .card.blue-grey.darken-1
        .card-content.white-text
          %span.card-title #{student.name}
          %p= student.email
          %p [ENROLLMENTS PLACEHOLDER]
        .card-action
          = link_to 'View Profile', '#'
